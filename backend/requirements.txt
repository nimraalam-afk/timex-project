# Backend dependencies for the Timex Scout MVP.
# Kept intentionally small: web framework, data models, server, and the OpenAI
# client (used later by the recommender/evaluator; safe to install now).
fastapi==0.115.0
uvicorn==0.30.6
pydantic==2.9.2
openai==1.51.0
